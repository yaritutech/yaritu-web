// Central site configuration values used across the frontend
// Include country code, no + or 00 prefix. Assuming India (91).
export const WHATSAPP_NUMBER = '918401721212';

export default {
  WHATSAPP_NUMBER,
};
